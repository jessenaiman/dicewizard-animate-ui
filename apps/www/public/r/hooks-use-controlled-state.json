{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "hooks-use-controlled-state",
  "type": "registry:ui",
  "title": "useControlledState",
  "description": "A hook that allows you to control a state.",
  "files": [
    {
      "path": "registry/hooks/use-controlled-state/index.tsx",
      "content": "import * as React from 'react';\n\ntype OnChangeOneArg<T> = (value: T) => void;\ntype OnChangeTwoArgs<T, E extends Event = Event> = (value: T, event: E) => void;\n\ninterface CommonControlledStateProps<T> {\n  value?: T;\n  defaultValue?: T;\n}\n\nexport function useControlledState<T>(\n  props: CommonControlledStateProps<T> & {\n    onChange?: OnChangeOneArg<T>;\n  },\n): readonly [T, (next: T) => void];\n\nexport function useControlledState<T, E extends Event = Event>(\n  props: CommonControlledStateProps<T> & {\n    onChange?: OnChangeTwoArgs<T, E>;\n  },\n): readonly [T, (next: T, event: E) => void];\n\nexport function useControlledState<T, E extends Event = Event>(\n  props: CommonControlledStateProps<T> & {\n    onChange?: OnChangeOneArg<T> | OnChangeTwoArgs<T, E>;\n  },\n): readonly [T, (next: T, event?: E) => void] {\n  const { value, defaultValue, onChange } = props;\n\n  const [state, setInternalState] = React.useState<T>(\n    value !== undefined ? value : (defaultValue as T),\n  );\n\n  React.useEffect(() => {\n    if (value !== undefined) {\n      setInternalState(value);\n    }\n  }, [value]);\n\n  const setState = React.useCallback(\n    (next: T, event?: E) => {\n      setInternalState(next);\n      if (!onChange) return;\n      if (onChange.length === 2) {\n        (onChange as OnChangeTwoArgs<T, E>)(next, event as E);\n      } else {\n        (onChange as OnChangeOneArg<T>)(next);\n      }\n    },\n    [onChange],\n  );\n\n  return [state, setState] as const;\n}\n",
      "type": "registry:ui",
      "target": "components/animate-ui/hooks/use-controlled-state.tsx"
    }
  ]
}